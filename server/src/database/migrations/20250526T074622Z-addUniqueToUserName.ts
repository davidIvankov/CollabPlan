import type { Kysely } from 'kysely'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function up(db: Kysely<any>) {
  // empty migration - no changes made
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function down(db: Kysely<any>) {
  // empty migration - no changes made
}
